from flask import Flask
from flask_sqlalchemy import SQLAlchemy

app = Flask(__name__)
app.config['SQLALCHEMY_DATABASE_URI'] = 'postgresql://postgres:f#P%SK4mj1461z!e3XF*@localhost:5432/Airbnb_NYC'
db = SQLAlchemy(app)

# Define your database models here using SQLAlchemy ORM

@app.route('/')
def hello():
    return 'Test'

if __name__ == '__main__':
    app.run(debug=True)
